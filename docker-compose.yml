version: '3.6'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: web

    volumes:
       - ./config:/server/config:ro
       - ./pyweb:/server/pyweb:ro

    ports:
      - 4445:8000

    command:
      python start.py
