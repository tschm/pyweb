version: '3.6'
services:
  sut:
     build:
       context: .
       dockerfile: Dockerfile
       target: test

     volumes:
        - ./test:/pyweb/test:ro
        - ./artifacts:/pyweb/artifacts

     links:
       - test-postgresql

     user: root

  test-postgresql:
    image: postgres:9.6
